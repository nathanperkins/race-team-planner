# Environment variables for integration tests (npm run test:integration).
# This file is loaded automatically by the integration test runner.
# It is safe to commit — all values are test-only, no secrets.

# Test database — separate from the dev database to avoid wiping real data.
# The test runner creates this database automatically if it does not exist.
DATABASE_URL_TEST=postgresql://postgres:password@localhost:5433/race-team-planner-test

# Required by Prisma field encryption at client instantiation time.
# This is a throwaway key only used in tests — not a real secret.
PRISMA_FIELD_ENCRYPTION_KEY=k1.aesgcm256.AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=

# Used by sendTeamsAssignmentNotificationWithData to build event URLs.
NEXTAUTH_URL=http://localhost:3000
